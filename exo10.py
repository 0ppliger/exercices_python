coord_a = { 
    "x": 45,
    "y": 32
 }

coord_b = { 
    "x": 54,
    "y": 28
 }

def get_highest(coord_a: dict, coord_b: dict) -> dict:
    # Highest is the greatest Y value
    return coord_a

def get_farest(coord_a: dict, coord_b: dict) -> dict:
    # Farest is the greatest X value
    return coord_a

def get_mid(coord_a: dict, coord_b: dict) -> dict:
    # Mid is between A and B
    return coord_a
